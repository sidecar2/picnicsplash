import type { ControllerState, SliderConfig } from "@/types";

function mid(min: number, max: number): number {
  return (min + max) / 2;
}

export const DEFAULT_STATE: ControllerState = {
  courierCount: mid(10, 120),
  demandVolume: mid(50, 400),
  avgCourierSpeedMph: mid(8, 20),
  dispatchHoldWindowMinutes: mid(0, 10),
  batchingRadiusMiles: mid(0.1, 1.5),
  targetAvgBatchSize: mid(1.0, 3.0),
  parkingHandoffTimeMinutes: mid(0.5, 6.0),
  prepTimeMeanMultiplier: mid(0.6, 1.6),
  prepTimeVarianceMultiplier: mid(0.5, 2.0),
  badActorRestaurantsPct: mid(0, 30),
  courierCostPerHour: mid(16, 35),
  internalContributionPerOrder: mid(-1, 6),
  partnerContributionPerOrder: mid(-2, 4),
  selectionContributionPerOrder: mid(-4, 2),
  refundRatePct: mid(0, 8),
  avgRefundCost: mid(5, 25),
};

export const SLIDER_CONFIGS: SliderConfig[] = [
  {
    key: "courierCount",
    label: "Courier Count",
    min: 10,
    max: 120,
    step: 1,
    caption: "Number of couriers available in the zone.",
    format: (v) => Math.round(v).toString(),
  },
  {
    key: "demandVolume",
    label: "Demand Volume (orders/hour)",
    min: 50,
    max: 400,
    step: 5,
    caption: "Total order demand per hour.",
    format: (v) => Math.round(v).toString(),
  },
  {
    key: "avgCourierSpeedMph",
    label: "Avg Courier Speed (mph)",
    min: 8,
    max: 20,
    step: 0.5,
    caption: "Average courier travel speed.",
    format: (v) => v.toFixed(1),
  },
  {
    key: "dispatchHoldWindowMinutes",
    label: "Dispatch Hold Window (min)",
    min: 0,
    max: 10,
    step: 0.5,
    caption: "Time to hold orders before dispatch for batching.",
    format: (v) => v.toFixed(1),
  },
  {
    key: "batchingRadiusMiles",
    label: "Batching Radius (miles)",
    min: 0.1,
    max: 1.5,
    step: 0.1,
    caption: "Max distance to batch orders together.",
    format: (v) => v.toFixed(1),
  },
  {
    key: "targetAvgBatchSize",
    label: "Target Avg Batch Size",
    min: 1.0,
    max: 3.0,
    step: 0.1,
    caption: "Target orders per batch.",
    format: (v) => v.toFixed(1),
  },
  {
    key: "parkingHandoffTimeMinutes",
    label: "Parking + Handoff Time (min)",
    min: 0.5,
    max: 6.0,
    step: 0.1,
    caption: "Avg time at drop-off per order.",
    format: (v) => v.toFixed(1),
  },
  {
    key: "prepTimeMeanMultiplier",
    label: "Prep Time Mean Multiplier",
    min: 0.6,
    max: 1.6,
    step: 0.05,
    caption: "Multiplier on restaurant prep time mean.",
    format: (v) => v.toFixed(2),
  },
  {
    key: "prepTimeVarianceMultiplier",
    label: "Prep Time Variance Multiplier",
    min: 0.5,
    max: 2.0,
    step: 0.05,
    caption: "Multiplier on prep time variance.",
    format: (v) => v.toFixed(2),
  },
  {
    key: "badActorRestaurantsPct",
    label: '"Bad Actor" Restaurants (%)',
    min: 0,
    max: 30,
    step: 1,
    caption: "Share of restaurants that are slower.",
    format: (v) => `${Math.round(v)}%`,
  },
  {
    key: "courierCostPerHour",
    label: "Courier Cost ($/hour)",
    min: 16,
    max: 35,
    step: 0.5,
    caption: "Cost per courier per hour.",
    format: (v) => `$${v.toFixed(1)}`,
  },
  {
    key: "internalContributionPerOrder",
    label: "Internal Contribution ($/order)",
    min: -1,
    max: 6,
    step: 0.25,
    caption: "Contribution margin per internal order.",
    format: (v) => `$${v.toFixed(2)}`,
  },
  {
    key: "partnerContributionPerOrder",
    label: "Partner Contribution ($/order)",
    min: -2,
    max: 4,
    step: 0.25,
    caption: "Contribution margin per partner order.",
    format: (v) => `$${v.toFixed(2)}`,
  },
  {
    key: "selectionContributionPerOrder",
    label: "Selection Contribution ($/order)",
    min: -4,
    max: 2,
    step: 0.25,
    caption: "Contribution margin per selection order.",
    format: (v) => `$${v.toFixed(2)}`,
  },
  {
    key: "refundRatePct",
    label: "Refund Rate (%)",
    min: 0,
    max: 8,
    step: 0.25,
    caption: "Share of orders that result in a refund.",
    format: (v) => `${v.toFixed(1)}%`,
  },
  {
    key: "avgRefundCost",
    label: "Avg Refund Cost ($/refund)",
    min: 5,
    max: 25,
    step: 0.5,
    caption: "Average cost per refund.",
    format: (v) => `$${v.toFixed(1)}`,
  },
];
